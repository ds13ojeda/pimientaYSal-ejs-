<!DOCTYPE html>
<html>
  <%- include('./partials/header') %>
    <!-- Page content -->
    <div class="ps-content">

      <!-- Menu Section -->
      <div class="ps-row ps-padding-64" id="menu">
        <div class="ps-col l6 ps-padding-large">
          <h1 class="ps-center">
            <%= searchedPlate.name %>
          </h1>
          <br />
          <h4>
          <% if (searchedPlate.detail != "") { %>
            <%= searchedPlate.detail %>
          <% } else { %>
            <h4> Plato típico </h4>
          <% } %>
          </h4>
            <p class="ps-text-grey"> 
              <%= searchedPlate.price %> 
            </p>
        </div>
        <div class="ps-col l6 ps-padding-large ps-img-menu">
          <img src="/images/menu.jpg" class="ps-round ps-image ps-opacity-min" alt="Menu"/>
        </div>
      </div>

      <!-- Footer -->
      <footer class="ps-center ps-light-grey ps-padding-32">
        <p>Powered by Pimienta & Sal</p>
      </footer>
    </div>
  </body>
</html>

